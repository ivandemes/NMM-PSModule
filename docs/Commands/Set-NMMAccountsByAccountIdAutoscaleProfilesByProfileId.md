# Set-NMMAccountsByAccountIdAutoscaleProfilesByProfileId

## Synopsis

Update the account auto-scale profile

## Syntax

~~~powershell
Set-NMMAccountsByAccountIdAutoscaleProfilesByProfileId -ProfileId <integer> -AccountId <string> [-InputObject <object>] [-Connection <psobject>]
~~~

## Description

No additional description is provided by the API specification.

## API operation

PUT /rest-api/v1/accounts/{accountId}/autoscale-profiles/{profileId}
